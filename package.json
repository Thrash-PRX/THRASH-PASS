{
  "name": "thrash-pass",
  "version": "2.1.3",
  "private": true,
  "scripts": {
    "test": "node scripts/validate.mjs && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
